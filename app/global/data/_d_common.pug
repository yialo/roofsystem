-
  const SITE_TITLE = 'Руфсистем';
  const SITE_DESCRIPTION = `Сайт компании ${SITE_TITLE}`;
  const PAGES = {
    index: 'Главная',
    about: 'О компании',
    gallery: 'Галерея',
    price: 'Цены',
    contacts: 'Контакты',
  };
  const FORM_DEFAULT_ACTION = 'https://echo.htmlacademy.ru';
  const _TEL = '+78008008990';
  const _TEL_TEXT = _TEL.slice(0, 2)
    .concat(' ', _TEL.slice(2, 5), ' ', _TEL.slice(5, 8), ' ', _TEL.slice(8));
